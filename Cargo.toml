[package]
name = "RayTrace"
version = "0.1.0"
edition = "2021"

[dependencies]
image = "0.25.5"
glam = { version = "0.29", features = ["serde"] }

serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

pbr = "1.1" # Progress bar
